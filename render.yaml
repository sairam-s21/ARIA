services:
  - type: web
    name: aria-ai-service
    runtime: python
    rootDir: AI_stuff
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.intent_api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: NVIDIA_API_KEY
        sync: false
